export default function ChartPlaceholder({ label }) {
    return (
        <div className="h-52 rounded-xl bg-gray-100 dark:bg-zinc-800
                    flex items-center justify-center text-sm text-gray-400">
            {label}
        </div>
    );
}
